export namespace µFeature {
  export interface Props {
    title: string
    children: React.ReactNode
    icon: React.ReactElement
  }
}

export namespace µTestimonial {
  export interface Props {
    image: string
    name: string
    role: string
    children: React.ReactNode
  }
}
